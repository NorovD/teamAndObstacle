package Lesson_1.Marathon;


public class Dog extends Animal {
    public Dog(String type, String name, int maxRunDistance, int maxJumpHeight, int maxSwimDistance,boolean onDistance) {
        super(type, name, maxRunDistance, maxJumpHeight, maxSwimDistance, onDistance);
    }
}